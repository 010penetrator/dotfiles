cd /ln/hh/yt
if [[ $2 ]]
	then a="bestvideo[height<=$1]+bestaudio"
	echo format= $a
	youtube-dl -f "$a" $2
	else if [[ $1 ]]
		then youtube-dl $1
	fi
fi
echo .
echo -n 'output= ' 
ls -lh /ln/hh/yt/*"$(echo "$@" | cut -d = -f2)".*
cd - &>/dev/null


cd /ln/hh/yt || cd /ln/dwn
if ! [ -z $2 ]
	then a="bestvideo[height<=$1]+bestaudio"
	echo format= $a
	youtube-dl -f "$a" $2
	else if ! [ -z $1 ]
		then youtube-dl $1
	fi
fi
echo .
echo -n 'output= ' 
ls -lh "$PWD"/*"$(echo "$@" | cut -d = -f2)".*
cd - &>/dev/null

